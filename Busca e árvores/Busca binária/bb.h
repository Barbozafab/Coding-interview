// Implementa a busca binária iterativamente,
// retornando o índice do elemento no vetor, se existir, -1 caso contrário
int bb_iterativa(int* vetor, int num, int tamanho);

// Implementa a busca binária iterativamente,
// retornando o índice do elemento no vetor, se existir, -1 caso contrário
int bb_recursiva(int* vetor, int num, int inicio, int fim);
